# OpenVPN Traffic Monitor Configuration
# ===========================================

# Management Interface Settings
OPENVPN_MANAGEMENT_HOST=127.0.0.1
OPENVPN_MANAGEMENT_PORT=7505

# Monitor Service Settings  
MONITOR_INTERVAL=45
MAX_LOG_SIZE=10485760

# File Paths
OPENVPN_LOG_FILE=/var/log/openvpn/traffic_monitor.log

# Project Settings (automatically detected if not set)
# PROJECT_ROOT will be set automatically by deploy.sh
# If running manually, set this to your project directory
# PROJECT_ROOT=/etc/openvpn

# Directory Paths (all relative to PROJECT_ROOT for consistency)
# OPENVPN_DIR will be set to PROJECT_ROOT/openvpn_data
# This keeps everything in one place and avoids confusion
VAR_LOG_OPENVPN=/var/log/openvpn
VAR_RUN_OPENVPN=/var/run/openvpn

# Performance Settings
# CHECK_INTERVAL must be between 30-60 seconds for optimal performance
# MAX_LOG_SIZE is in bytes (default: 10MB)

# Notes:
# - Restart the openvpn-monitor service after changing these values
# - Use: sudo systemctl restart openvpn-monitor